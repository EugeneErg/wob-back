<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * У главы появляется своя картинка на доске истории.
 *
 * Это всё. Ни планеты, ни острова тут нет — и не должно быть: планета это
 * история с такой обложкой, остров это глава с такой картинкой. Признака «эта
 * история — планета» не существует, потому что различать нечего. В другой
 * истории главы будут кнопками, в третьей — дверями, и функция у них одна.
 *
 * Сам я сперва завёл и планету отдельным полем, и развилку «если планета, то
 * показываем один экран, иначе другой». Развилка была лишней дважды: она
 * делила то, что не делится, и заодно прятала экран глав от историй без
 * картинки — хотя главы у них тоже есть и выбирать их тоже надо.
 *
 * Поворота здесь тоже нет: повёрнутый значок — это повёрнутая картинка, и
 * поворачивают её там же, где рисуют. Поле под угол было бы вторым способом
 * сделать то, что умеет любой рисовальщик.
 *
 * Умолчание сохраняет нынешнее поведение: без картинки глава остаётся
 * прямоугольником, каким и была.
 */
return new class () extends Migration {
    public function up(): void
    {
        Schema::table('chapters', static function (Blueprint $table): void {
            // Ссылкой на носитель, как обложка истории и фон карты главы.
            $table->string('icon', 2000)->default('');
        });
    }

    public function down(): void
    {
        Schema::table('chapters', static function (Blueprint $table): void {
            $table->dropColumn(['icon']);
        });
    }
};
